version: "2.2"

services:
    strumbot-min:
        image: minnced/strumbot:1.3.2-min
        build:
            context: ..
            dockerfile: containers/minimal/Dockerfile
    strumbot:
        image: minnced/strumbot:1.3.2
        build:
            context: ..
            dockerfile: containers/normal/Dockerfile
